# Handover POS — 24 Agustus 2026

## Lingkup audit

- Backend Java AIS: `C:\opt\AIS\ais\src\main\src`
- Client Desktop/Android Flutter: `C:\opt\CodeBaseDesktopDanMobile`
- Backend tetap kompatibel Java 1.7 dengan gaya Java 1.6.

## Perubahan sesi lain yang diaudit

1. Transaksi offline yang membawa kode sesi lama/tidak dikenal tidak lagi
   otomatis ditempel ke sesi yang sedang aktif. Server mencari sesi kasir dan
   toko yang rentang waktunya mencakup waktu transaksi, termasuk sesi yang
   sudah ditutup. Jika tidak ditemukan, perilaku lama (sesi aktif) tetap menjadi
   fallback.
2. Pencarian sesi historis mengabaikan identitas kasir kosong dan berhenti aman
   bila nama maupun user ID tidak tersedia.
3. Jenis member dengan daftar metode pembayaran `NULL`/kosong diperlakukan
   sebagai konfigurasi yang belum diisi, bukan larangan semua metode. Transaksi
   tetap dapat berjalan memakai semua metode aktif.
4. API mengirim `izinTidakDisetel=true`; client menampilkan peringatan
   non-blocking agar admin dapat memperbaiki konfigurasi tanpa menghentikan
   kasir.

## Kontrak yang dipertahankan

- Daftar izin yang berisi nilai tetapi tidak cocok tetap menghasilkan daftar
  kosong karena itu merupakan pembatasan admin yang disengaja.
- `openSession()` pada jalur yang diperiksa ditutup melalui `finally` dan
  `HibernateUtil.closeSessionQuietly(session)`.
- Payload dan perilaku client lama tetap kompatibel; field respons baru bersifat
  tambahan.

## Verifikasi

- `javac -source 1.7 -target 1.7` untuk `LaporanRincianTransaksiUtil`,
  `SesiKasUtil`, `PosApi`, dan `KantinHelper`: **lulus**. Satu peringatan
  bootstrap classpath JDK bersifat informatif; tidak ada error kompilasi.
- `flutter analyze lib test`: tidak ada error. Terdapat 51 info dan satu warning
  lama di modul lain; perubahan `keranjang_screen.dart` tidak menambah error.
- `flutter test`: **seluruh suite lulus** (`exit 0`).
- Tes kontrak terfokus `master_offline_kontrak_test.dart` dan
  `toko_id_payload_test.dart`: **24 tes lulus**.
- Backend sudah masuk SVN revisi **r78227** dengan pesan
  `Perbaiki rekonsiliasi sesi kas dan fallback metode pembayaran member`.
- Perubahan client dan dokumen ini masuk ke commit Git yang memuat dokumen ini;
  hash dan status push dicantumkan pada laporan akhir sesi.

## Aturan handover berikutnya

1. Audit `git status`, `svn status -u`, dan perubahan remote sebelum menyunting,
   karena modul POS dapat dikerjakan beberapa sesi secara bersamaan.
2. Catat perubahan perilaku, kontrak API, pengujian, revisi SVN, dan commit Git
   baru di `docs/pos/`.
3. Jangan menghapus fallback sesi aktif: pencarian sesi historis hanya
   mendahuluinya untuk transaksi lokal-dulu yang datang terlambat.
4. Jangan mengubah daftar izin member yang terisi tetapi tidak cocok menjadi
   semua metode; fallback semua metode hanya berlaku bila daftar izin benar-benar
   kosong/belum disetel.
